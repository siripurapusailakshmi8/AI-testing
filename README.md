Automation test suite (Selenium, Python) for OFD test cases.

Run tests with pytest after installing requirements:

pip install -r requirements.txt
pytest -q

Configure environment variables for BASE_URL, TEST_USERNAME, TEST_PASSWORD, ORDER_API_BASE, TRACKING_MOCK_API as needed.
